# Macro-Eval Sweep — 2026-06-08
Window: last 4 days · Population: **12 runs (window: 0 — expanded to full DB)** · Filter: all

> **Note — window expansion:** The 4-day window (2026-06-04 to 2026-06-08) returned 0 runs. All 12 available runs date from 2026-05-19–20. Sweep was run over the full population with "below density threshold — results indicative only" flag applied. No recent activity means the findings describe **bootstrap/initial instrumentation state**, not a live production baseline.

---

## Population summary

| Run ID (abbrev) | Event | Date | Spans | Errors | Functional outcome |
|---|---|---|---|---|---|
| test-v1 | test-event | 2026-05-19 | 1 | 0 | successful_completion (setup) |
| 24a733f0 | purina-poc | 2026-05-19 | 1 | 0 | successful_completion |
| smoke-1 | zora-engine | 2026-05-19 | 1 | 0 | successful_completion (smoke) |
| smoke-2 | purina-poc | 2026-05-19 | 1 | 0 | successful_completion (smoke) |
| smoke-engine-fixed | zora-engine | 2026-05-19 | 1 | 0 | successful_completion (smoke) |
| a859a6be | purina-poc | 2026-05-20 | 1 | 0 | **review_escalation** |
| cbd52761 | purina-poc | 2026-05-20 | 1 | 0 | **review_escalation** |
| ad42437f | purina-poc | 2026-05-20 | 1 | 0 | successful_completion |
| 596a4c52 | purina-poc | 2026-05-20 | 1 | 0 | **review_escalation** |
| 6b092390 | purina-poc | 2026-05-20 | 1 | 0 | successful_completion |
| 6b4473c2 | purina-poc | 2026-05-20 | 1 | 0 | **review_escalation** |
| af796e97 | purina-poc | 2026-05-20 | 1 | 0 | successful_completion |

Outcome classification is **content-level** (all span statuses are OK; refusals are functional failures not span-level errors).

---

## Pattern leaderboard

| Rank | Behavior pattern | Runs | Outcome mix | Impact score | Suspect step |
|------|-----------------|------|-------------|-------------|--------------|
| 1 | **Knowledge refusal on domain queries** | 4 | 0 success / 4 escalation | **2.67** | Missing data grounding in LLM context |
| 2 | Smoke / infrastructure setup | 4 | 4 success / 0 fail | 1.33 | *(context only)* |
| 3 | Zero-value structured answer | 2 | 2 success / 0 fail | 0.33 | *(context — likely empty test data)* |
| 4 | Factual completion | 2 | 2 success / 0 fail | 0.33 | *(context)* |

Impact formula: `prevalence_share × Σ(severity_weight)` where successful_completion=1.0, review_escalation=2.0, hard_failure=2.5.

---

## Diagnoses (top pattern)

### Pattern 1 — Knowledge refusal on domain queries — impact 2.67

**Members (4 runs, all within annotation cap):**
- `a859a6be` — "What is driving the ISR variance between GMP and VFP this period?"
- `cbd52761` — "What is the total CSM balance and how has it moved since last period?"
- `596a4c52` — "What is the net reinsurance impact on ISR across all classifications?"
- `6b4473c2` — "What is the total CSM balance and how has it moved since last period?"

**Focus event:** Single LLM span (`claude_code_session`) — output is bare refusal string: `"I don't have enough information to answer this query."`

**Suspect step (inspection guide, not proof):**
- Suspect: **data grounding / RAG injection step is absent or failing for these queries**
- Suspect score: proximity=1.0 (only 1 span per run), frequency=4/9 production-like runs, bridge=0.5 (same question on CSM alternates refusal/answer — see sub-pattern), role=1.0 (LLM is the terminal step)
- Evidence: The CSM balance question was asked 4 times. Two runs (`cbd52761`, `6b4473c2`) returned refusals; two runs (`ad42437f`, `af796e97`) returned formatted $0 answers. **This non-determinism is the clearest signal** — identical input, split outcome. Either (a) the grounding pipeline is flaky, or (b) two different agent versions were tested back-to-back (pre/post fix).

**Sub-pattern — CSM non-determinism (2 refusal + 2 zero-value):**
```
08:12 cbd52761  → "I don't have enough information"
08:38 ad42437f  → "Total CSM balance is $0.0, with no movement"   ← grounded
10:58 6b4473c2  → "I don't have enough information"
10:59 af796e97  → "Total CSM balance is $0, unchanged"             ← grounded
```
The alternating pattern (refusal → grounded → refusal → grounded) within a ~3-hour window suggests iterative debugging of the data grounding pipeline during the 2026-05-20 session. The grounded runs may represent a short-lived fix that was not yet stable.

**Recommended inspection:**
1. Check grounding/RAG pipeline code for the `aiq-purina-salesorderintelligence-poc` agent around 2026-05-19–20.
2. Confirm whether `ad42437f` and `af796e97` had a different prompt template or tool configuration than the refusal runs.
3. The bare refusal string ("I don't have enough information to answer this query.") appears to be a hard-coded fallback — trace where it is emitted vs. a natural LLM refusal.

---

## Notes on other patterns

**Smoke/infrastructure (4 runs):** `test-v1`, `smoke-1`, `smoke-2`, `smoke-engine-fixed` are connectivity/bootstrap tests. Normal. No action needed.

**Zero-value answers (2 runs):** `ad42437f` and `af796e97` returned formatted but empty data ($0 CSM balance, no movement). Likely correct behavior against a synthetic/test dataset — not a failure.

**Factual completion (2 runs):** `24a733f0` (Chicago Q4 orders) and `6b092390` (no onerous contracts) completed cleanly with substantive or correctly-empty answers.

---

## Delta vs previous sweep

**First sweep — no baseline.** This is the inaugural run. The 12 runs in the DB represent bootstrap/instrumentation setup from 2026-05-19–20. No production traffic has been observed in Workshop yet. Recommend re-running this sweep once live aiq-purina-poc traffic is instrumented.

---

## Action items

1. **Instrument live traffic** — Raindrop has only test/smoke data. Wire the production Purina POC calls through the Workshop tracer to get real signal.
2. **Investigate the CSM non-determinism** — the alternating refusal/answer pattern on the same query in the 2026-05-20 session is the highest-value diagnostic lead.
3. **Harden the grounding fallback** — the bare "I don't have enough information" string is an opaque failure mode; a structured error span would surface in span-level monitoring automatically.

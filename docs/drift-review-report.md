## Harness Drift Review — 2026-08-18

### Auto-Fixed
- MEMORY: nothing to fix — no MEMORY.md or memory files exist for this project yet (clean slate)
- HOOKS: no dead references found — all hooks registered in settings.json exist on disk
- DOCS: no broken links found — all 17 markdown cross-links in docs/ resolve correctly

### Flagged for Human Review

**14 hook scripts on disk are not registered in `.claude/settings.json`**

These may be intentionally dormant, pending registration, or forgotten. Each script has a declared purpose in its header — review whether it should be wired up.

| Script | Lines | Declared event / matcher | Notes |
|--------|-------|--------------------------|-------|
| `agent-trace-hook.sh` | 87 | PostToolUse (Agent) | Header says "trace.log had zero reliable producers" — likely intentionally disabled |
| `ai-writing-guard-hook.sh` | 162 | PreToolUse Write\|Edit\|Bash — HARD DENY | Blocks AI-sounding buzzwords before file writes; appears production-ready |
| `frontend-security-nudge.sh` | 60 | UserPromptSubmit | Nudges frontend/UI/design tasks; parallel to `doc-parse-nudge.sh` which IS registered |
| `lean-ctx-nudge-hook.sh` | 73 | PostToolUse (Read) | Suggests optimal ctx_read mode for large files; Read is currently unlisted in PostToolUse |
| `pr-auto-create-hook.sh` | 48 | PostToolUse (Bash) | Auto-creates PR after plain `git push`; PostToolUse(Bash) already has 3 hooks |
| `pr-mention-nudge.sh` | 36 | UserPromptSubmit | Detects PR mentions when no PR exists yet |
| `pre-tool-use-gitnexus.sh` | 56 | PreToolUse (Read\|Edit) | Injects GitNexus blast-radius context before reads/edits |
| `prompt-hook.sh` | 15 | UserPromptSubmit | Reads hot-memory before each prompt; UserPromptSubmit already has `doc-parse-nudge.sh` |
| `raindrop-best-practices.sh` | 28 | PreToolUse (mcp__raindrop__) | Fires on Raindrop MCP calls; matcher would need to be `mcp__raindrop__.*` |
| `reject-feedback-hook.sh` | 121 | UserPromptSubmit | Handles rejected/interrupted tool calls; soft gate |
| `scan-pii.sh` | 121 | (standalone CLI) | Used via `--staged` flag for git staging; not hook-wired by design |
| `skill-usage-tracker.sh` | 58 | PostToolUse (Skill) | Appends per-invocation JSON for skill-curator; `Skill` matcher not currently in use |
| `tool-failure-capture.sh` | 126 | PostToolUse failure (Bash\|mcp__.*) | Records failing tool calls; needs `PostToolUseFailure` event (not `PostToolUse`) |
| `tool-failure-recall.sh` | 126 | PreToolUse (Bash\|mcp__.*) | Replays prior failures before retry; PreToolUse(Bash) already has `git-destructive-guard` |

**Recommended actions (prioritised):**
1. **Wire or formally retire** `ai-writing-guard-hook.sh` — it's a hard-deny gate with no registration; either intent is active or the hook is dead weight.
2. **Wire** `tool-failure-capture.sh` + `tool-failure-recall.sh` — they form a pair under `PostToolUseFailure` / `PreToolUse`; both are production-grade and currently no-ops.
3. **Confirm intentional** `agent-trace-hook.sh` — header suggests it was disabled; if so, move it to an `_archive/` folder to make the intent explicit.
4. **Review** `skill-usage-tracker.sh` — `PostToolUse` with matcher `Skill` is valid; low cost, high curator value.
5. **Defer** `scan-pii.sh` — standalone CLI, not hook architecture; no action needed.

### Clean (no drift found)

- **MEMORY.md**: no project memory directory exists — nothing stale to remove or index
- **Registered hooks → disk**: all 23 hooks referenced in `.claude/settings.json` exist on disk ✓
- **User-level hooks**: all 3 hooks in `~/.claude/settings.json` (`caveman-activate.js`, `caveman-mode-tracker.js`, `caveman-statusline.sh`) exist ✓
- **Doc links**: all 17 cross-references in `docs/**/*.md` resolve to existing files ✓

---

*Generated by `repo-drift-review` skill · next scheduled sweep: Wednesday*
